{% from "../../components/basket-item/basket-item.nunj" import basketItem %}

<h2 class="basket__heading">My basket</h2>
<ul class="basket-items">
	{% for item in basketItems %}
		{{ basketItem(item) }}
	{% endfor %}
</ul>
